'use strict';

import React from 'react';
import {AppRegistry, StyleSheet, View, Text, Image, StatusBar} from 'react-native';

let Walkthrough = React.createClass({
  render() {
    return(
      <Text>This is Walkthrough</Text>
    );
  }
})

module.exports = Walkthrough;
